<!-- Generated by scripts/adapter_contract.py. Do not edit by hand. -->

# Adapter Contract

This page is generated from the Python bus-functional adapter sources and checked by the Python-model CI lane.

## Summary

- Adapter classes: 5
- Shared conformance methods: 6
- Register-wrapper helper methods: 7
- AXI-Lite-specific helper methods: 5
- The direct-core adapter is allowed to omit shadow-program readback because the core programming port is write-only.

## Adapter Sources

| Adapter | Protocol views | Class | Source | Notes |
|---|---|---|---|---|
| `direct-core` | `Direct RTL programming port` | `SimpleCpuCoreBus` | `tb/core_bus.py` | Direct core adapter uses the CPU programming pins and has no readable shadow program. |
| `mmio` | `MMIO`<br>`MMIO wait` | `SimpleCpuMmioBus` | `tb/mmio_bus.py` | Shared register-map helper used by the always-ready and wait-state MMIO shells. |
| `apb` | `APB` | `SimpleCpuApbBus` | `tb/apb_bus.py` | APB setup/access helper over the shared wrapper address map. |
| `wishbone` | `Wishbone` | `SimpleCpuWishboneBus` | `tb/wishbone_bus.py` | Wishbone CYC/STB/ACK helper over the shared wrapper address map. |
| `axi-lite` | `AXI-Lite` | `SimpleCpuAxiLiteBus` | `tb/axi_lite_bus.py` | Tutorial AXI-Lite subset helper with coupled AW/W acceptance and one outstanding response. |

## Method Matrix

| Method | Expected kind | Scope | Notes | `direct-core` | `mmio` | `apb` | `wishbone` | `axi-lite` |
|---|---|---|---|---|---|---|---|---|
| `reset` | `async` | shared conformance | Put the DUT in a known idle state. | `async` | `async` | `async` | `async` | `async` |
| `load_program` | `async` | shared conformance | Load up to 16 instruction bytes. | `async` | `async` | `async` | `async` | `async` |
| `begin_execution` | `async` | shared conformance | Release or start execution after programming. | `async` | `async` | `async` | `async` | `async` |
| `run_until_halt` | `async` | shared conformance | Wait for HALT with a cycle budget. | `async` | `async` | `async` | `async` | `async` |
| `sample_state` | `async` | shared conformance | Return an MmioSnapshot-compatible final state. | `async` | `async` | `async` | `async` | `async` |
| `read_dmem` | `async` | shared state sampling | Read one data-memory byte for snapshot construction. | `async` | `async` | `async` | `async` | `async` |
| `read` | `async` | register wrappers | Read one wrapper register. | - | `async` | `async` | `async` | `async` |
| `write` | `async` | register wrappers | Write one wrapper register. | - | `async` | `async` | `async` | `async` |
| `load_shadow_program` | `async` | register wrappers | Write shadow instruction memory without starting. | - | `async` | `async` | `async` | `async` |
| `verify_loaded_program` | `async` | register wrappers | Read back shadow instruction memory. | - | `async` | `async` | `async` | `async` |
| `wait_for_control_state` | `async` | register wrappers | Poll the control register state bits. | - | `async` | `async` | `async` | `async` |
| `start_program` | `async` | register wrappers | Set RUN through the control register. | - | `async` | `async` | `async` | `async` |
| `stop_program` | `async` | register wrappers | Clear RUN through the control register. | - | `async` | `async` | `async` | `async` |
| `program_word` | `async` | direct-core helpers | Pulse one programming write. | `async` | - | - | - | - |
| `start_program_word` | `sync` | direct-core helpers | Hold one programming write active for fault tests. | `sync` | - | - | - | - |
| `stop_program_word` | `async` | direct-core helpers | Release a held programming write. | `async` | - | - | - | - |
| `attempt_partial_write` | `async` | axi-lite helpers | Exercise the rejected partial-write channel cases. | - | - | - | - | `async` |
| `wait_write_accept` | `async` | axi-lite helpers | Wait for coupled AW/W acceptance. | - | - | - | - | `async` |
| `wait_write_response` | `async` | axi-lite helpers | Wait for the B channel OKAY response. | - | - | - | - | `async` |
| `wait_read_accept` | `async` | axi-lite helpers | Wait for AR acceptance. | - | - | - | - | `async` |
| `wait_read_response` | `async` | axi-lite helpers | Wait for the R channel OKAY response. | - | - | - | - | `async` |

## Contract Rules

1. Every adapter must expose the shared conformance methods so `tb/protocol_conformance.py` can run unchanged across direct core, MMIO, APB, Wishbone, and AXI-Lite tests.
2. Register-wrapper adapters must expose `read`, `write`, shadow load/readback, and control-state helpers because directed cocotb and pyuvm tests use those methods outside the shared scenario replay.
3. AXI-Lite keeps explicit wait helpers and partial-write probing because the tutorial wrapper intentionally implements a restricted AXI-Lite subset.
4. This catalog parses adapter source files statically, so it does not need cocotb installed to catch missing or synchronous methods in the Python-model lane.

Regenerate this page after intentional adapter API edits with:

```powershell
.\scripts\generate-adapter-contract.ps1
```

```bash
bash scripts/generate-adapter-contract.sh
```
